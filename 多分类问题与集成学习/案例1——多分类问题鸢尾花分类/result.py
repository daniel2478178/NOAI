"""
代码段 1 补全答案
y = Sample[:,Sample.shape[1]-1]
X = Sample[:,0:Sample.shape[1]-1]
np.c_[xx.ravel(), yy.ravel()]
'iris_2feature3label_train.xlsx'
clf.fit(X_train,y_train)

代码段 2 补全答案
np.c_[xx.ravel(), yy.ravel(),2*np.ones(len(xx.ravel())),3*np.ones(len(yy.ravel()))]
'iris_4feature3label_train.xlsx'
clf.fit(X_train,y_train)
"""
